<dir align = "center">

	MDN-API

</dir>

## Response

```js
{
  heading: 'for (JavaScript)',
  description: 'The for statement creates a loop that consists of three optional expressions, enclosed in parentheses and separated by semicolons, followed by a statement (usually a block statement) to be executed in the loop.The following for statement starts by declaring the variable i and initializing it to 0. It checks that i is less than nine, performs the two succeeding statements, and increments i by 1 after each pass through the loop.',
  raw: {
    html: '<section class="prog__container"><p>The for statement creates a loop that consists of three optional expressions, enclosed in parentheses and separated by semicolons, followed by a statement (usually a block statement) to be executed in the loop.</p><p>The following for statement starts by declaring the variable i and initializing it to 0. It checks that i is less than nine, performs the two succeeding statements, and increments i by 1 after each pass through the loop.</p><pre><code>for (var i = 0; i &lt; 9; i++) {\n' +
      ' console.log(i);\n' +
      ' // more statements\n' +
      '}\n' +
      '</code></pre></section>',
    parsed: 'The for statement creates a loop that consists of three optional expressions, enclosed in parentheses and separated by semicolons, followed by a statement (usually a block statement) to be executed in the loop.\n' +
      '\n' +
      'The following for statement starts by declaring the variable i and initializing it to 0. It checks that i is less than nine, performs the two succeeding statements, and increments i by 1 after each pass through the loop.\n' +
      '\n' +
      '    for (var i = 0; i < 9; i++) {\n' +
      '     console.log(i);\n' +
      '     // more statements\n' +
      '    }'
  },
  syntax: {
    raw: [
      'for (var i = 0; i < 9; i++) {',
      '',
      ' console.log(i);',
      '',
      ' // more statements',
      '',
      '}',
      '',
      ''
    ],
    markdown: '```js\n' +
      'for (var i = 0; i < 9; i++) {\n' +
      '\n' +
      ' console.log(i);\n' +
      ' // more statements\n' +
      '\n' +
      '}\n' +
      '\n' +
      '\n' +
      '```'
  },
  source: {
    site: 'MDN Web Docs',
      '\n' +
      '\n' +
      '```'
  },
  source: {
    site: 'MDN Web Docs',
    url: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for'
  }
}
```